{
  "name": "xianfeng-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "sh ./scripts/build-web.sh",
    "sync": "npm run build:web && cap sync",
    "sync:android": "npm run build:web && cap sync android",
    "sync:ios": "npm run build:web && cap sync ios",
    "copy": "npm run build:web && cap copy",
    "build:android:debug": "npm run sync:android && cd android && ./gradlew assembleDebug",
    "build:android:release": "npm run sync:android && cd android && ./gradlew assembleRelease",
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "doctor:env": "sh ./scripts/doctor.sh",
    "doctor": "cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^6.0.3"
  }
}
